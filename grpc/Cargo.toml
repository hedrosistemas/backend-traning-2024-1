[package]
name = "historian"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic = "0.10.2"
prost = "0.12.3"
tokio = { version = "1.35.1", features = ["macros", "rt-multi-thread"] }

[build-dependencies]
tonic-build = { version = "0.10.2" }

